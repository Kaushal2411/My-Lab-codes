#include<stdio.h>
int main() {
    int a =5,b=24; // In this line we declare and initialize two integer variables a and b
    printf("The sum of the number %d and %d is %d", a, b, a + b); // This line prints the sum of a and b
    return 0;
}
